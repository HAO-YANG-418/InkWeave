# InkWeave 二轮核验报告（trea 更新后实测）

> 核验时间：2026-08-17 02:49
> 方法：不信任"done"，实测文件改动 + 真跑检测工具 + 启动网页服务验证
> 对照：上一轮《InkWeave_验收核对报告.md》中"未做/有水分"项

---

## ✅ 已确认修复（实测通过）

### 1. 封装 #5 — 网页壳真实可用（最大进展）
- **产物**：`InkWeave/inkweave-web.js`（v4.8，8/17 02:17 生成）、`InkWeave/dist/` 编译目录
- **实测**：`node inkweave-web.js --port 4173` 启动成功，首页返回有效 HTML（`<title>InkWeave v4.8`）`/api/run` 接口可调起 `dist/` 下检测工具
- **含义**：非技术用户不必敲 `npx tsx`，浏览器点按钮即可做 检测 / 写前分析 / 快写 / 全链路流水线
- **残留**：
  - 仍要求本机装 Node（不是零安装）
  - 是本地 HTTP 服务，未托管、未做成可分发安装包
  - 未进 `~/.workbuddy/skills/`（不是 WorkBuddy 原生 skill）
  - 接口路径处理：同时传 `file`(全路径)+`project` 会重复拼接路径（如 `项目_裂日/章节/项目_裂日/章节/...`）；UI 传裸文件名则正常 → 建议 web 层做路径归一

### 2. 残留风险 A — 排比堆叠升为 error（硬约束打通）
- **代码**：`检测工具/checkers.ts:788` → `severity: totalStacks >= 6 ? 'error' : 'warning'`
- **含义**：单章同句式堆叠 ≥6 处即 `error`，**阻断生成**（之前只是 warning 软约束，下章照样能堆砌过关）
- 检测器同时覆盖：同动词排比、介词堆叠、"的"字密度、"X的Y"句式密集

### 3. 闭环指纹机制 — stylePatterns 真实写入
- **实测**：对第14章跑检测（`--save-fingerprint`）后，`第14章 溯源.fingerprint.json` 新增 `stylePatterns` 字段
- **含义**：写前分析可读取上章风格问题，注入下一章"本章禁忌"，**反向闭环管道确认通**

### 4. 版本号一致
- 四个 SKILL.md（主 / 写作 / 大纲 / 设定）均 v4.8，统一 ✅

---

## ❌ / ⚠️ 仍未解决

### 5. 效果度量 #7 — 仍无数据（卖课硬伤未补）
- 全仓无任何 盲评 / A-B / 完读率 / 修复前vs修复后 读者侧数据
- 仅检测侧计数（如"16组74处排比"）存在，无法证明"比 baseline 好 X%"

### 6. 闭环普及性（残留风险 B = 过程缺口，非代码坏）
- 15 章仅 5 个 `fingerprint.json`，且此前大多**无** `stylePatterns`
- 反向闭环要求"每一章的上章指纹含 stylePatterns"才能继承约束 → 需对全书跑一遍指纹重算才能铺满
- **机制通，但没跑完**：现仅 demo 级（单章点火），未系统性覆盖

### 7. 分发形态 — 网页壳为本地服务，未到"可卖"
- 要卖课还需三选一：托管为线上服务 / 打包成可一键安装包 / 上架 WorkBuddy skill 市场

---

## 给 trea 的下一步指令
1. **跑全量指纹重算**（check-all --save-fingerprint 或逐章），让每章 fingerprint 含 `stylePatterns`，闭环铺满全书 → 闭环从 demo 变生产
2. **补 #7 效果度量**：抓 ≥10 章做"修复前 vs 修复后"句式计数对比 + 读者侧盲评（哪怕小样本），用于卖课弹药
3. **定分发方案**：把网页壳做成可一键分发的形态（托管 / 安装包 / skill 市场上架）
4. **web 层路径归一**：避免 `file`+`project` 重复拼接

---

## 一句话结论
trea 这轮把"能不能用"（网页面板）和"硬约束"（排比 error）两件最关键的事**真做出来了、且实测能跑**；但"能不能卖"（度量 + 分发）还差最后两步。距离可交付变现，进度约 70%。
