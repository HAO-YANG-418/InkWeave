# 连章全章 0 error 交付（2026-09-02 收尾）

## 全局真实盘面（check-all 合并源码树后）
- 章节数：5 | 总字数：15,807
- 等级分布：**B级 1章（ch5=85） / D级 4章（ch01~04）**
- **全 5 章 0 error，质量门禁全部通过**（✅）

## 本轮完成的修复（用户授权："字数2800就行，其他你看着办"）
1. **验收线 2800 落地**：`node-registry.ts` 默认字数节点 `opt_length_2200`→`opt_length_2800`（新增节点 + `opt_length_2800.kb.json`），`base-prompt.ts` 字数指令改 2800，`kb-loader.ts` 三处注入。`npx tsc` 重建 dist，grep 验证三处补丁串均同步。
2. **门禁双路径统一（根除了"假全绿"）**：`check-all.ts` 原先只跑 CLI 树 `checkChapter`，藏起源码树 R1 聚合的 error/warning，导致同文件 ch5 在单章报 85/3警告、在 check-all 报 99/0警告 的打架。**现已让 check-all 合并源码树**，两条命令结论一致——这是此前"全5章0 error"假象的真凶。
3. **连章 4 章全部清 error**（规则真身 `源码/checker.ts:1671`：同一 <200字段内 ≥2 个"像/如/仿佛"短比喻 → error）：
   - ch01：拆 L19/L71/L43 "像"簇 → **0 error（D26）**
   - ch02：拆电话段 L35/37/43/47 + 全文叙述性"像"直述化（仅留对话内口语"像有痰卡着"）→ **0 error（D57）**
   - ch03：拆 L93 段真簇（"不像刀口"+"像在报情况"）+ 删水 3547→3285 → **0 error（D50）**
   - ch04：删水 3458→3203（精删 L15/L19/L21 回忆注水段）→ **0 error（D49）**
4. **破折号铁则守住**：删水时手滑写 3 处"——"（ch03 L47/L61/L79），当场改回逗号，未污染出货。破折号仍是硬禁 error。

## 残留（非阻断，不藏着）
- 4 章 D 级：警告密集（碎句 / 排比堆叠 / 段落过长 / 连续对话×N / 句子过长 / 台词差异化 quirk）。**门禁只看 error 级，D 级不影响出货**。
- 检测器两处已知 quirk 未动（动之有引入新 error 风险，留专项）：
  - 台词差异化统计自相矛盾（同章报"占比49%" vs 基础统计1.2%，且数的是标签类型非声口）
  - 段落过长阈值偏严（旧章长段写法易触发，非硬伤）

## 别倒退能力确认（5条已验证能力均未回潮）
- 口语人味范式（铁则十 + 铁则四放宽 + 铁则五性格驱动）✓
- 破折号硬禁 + 全角弯引号可见 ✓
- 0 error 门禁可出货 ✓（全 5 章实证）
- dist 同步铁律 ✓（改 `源码/kb/**` 必重建 + grep 验证）
- 8 本好懂口语化语料定稿 ✓

## 一句话总判
连章出货闭环已真正打通（5章全0 error、字数达标2800-3200、口语人味无倒退）。分数 D 级是旧章写法触发检测器警告所致，不阻断出货；ch5 为 B 级样板可主推。如需把连章整体提到 B 级，需降警告（碎句/排比/长段），属"提卖相"非"修缺陷"，待用户拍板再动。
