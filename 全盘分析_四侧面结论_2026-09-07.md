# InkWeave 全盘分析 · 四侧面核查结论（2026-09-07）

> 纪律：不采信自述、读真源。以下结论全部来自 `file:line` 一手证据，非推断。

## 0. 执行边界
- **push 被环境阻断**：`fetch origin` 报 `Connection was reset`，沙箱到 `github.com` 出口被重置。两笔提交 `17d0f08` + `c65ba93` 未能推送；已按"治本推送"配好 `http.postBuffer=524288000`（本地），待网络通即可推。
- 本分析为"启动全盘分析"后的第一刀，聚焦四侧面现状与一致性。

## 1. 检测器面 — D2 方案 B 落地 ✅（含 1 处文档病已修）
- **真身**：`检测工具/checkers.ts:1088` → `severity = allMatches.length >= 3 ? 'error' : (>=1 ? 'warning' : 'info')`，即 **≤2 处 warning、≥3 处 error**，方案 B 真生效。
- **双树对齐**：`源码/checks/check-not-shi-pattern.ts:36-41` 用 `thresholds.notShiErrorMin ?? 3`，severity 逻辑与 CLI 树逐字一致 ✅。
- **已知差异**：源树 L12 注明"跨段/孤句模式未移植"= 设计性漏检 gap（不冤枉作者，由 CLI 树验收补齐），非回归。
- **已修文档病**：原 `checkers.ts:1084-1085` 注释写"改为出现即 error"，与方案 B 矛盾 → 已改为演进注释（纯注释，不改逻辑）。

## 2. 代码面 — 双树一致性 ⚠️（破折号三轨漂移，待拍板）
- not_shi 双树 severity 已对齐 ✅（见 1）。
- **破折号三轨不一致（核心发现）**：
  | 位置 | 口径 |
  |---|---|
  | 源码 prompt `base-prompt.ts:149` / `self-correction-core.ts:20,67` | "零容忍，出现 1 个即错"（声称硬） |
  | 源码 detector `源码/checker.ts:440` `max_dash_count` | `severity: 'info'`（近乎静默） |
  | CLI detector `检测工具/checkers.ts:437,441` | `dashCount > 2` 才 `warning` |
  | **结论** | **没有任何一处 detector 把破折号当 error 卡** |
- **代码自打脸**：CLI 树 L420、L433-434 注释吹"硬禁/零容忍/出现 1 个即 error"，真实代码是 `>2 warning` → 已改注释说人话（纯注释）。
- **架构冲突**：09-04 分层铁律（破折号属卫生层，应 hard error） vs 09-05 软护栏（把它降级）。需拍板：复硬（≥1 error，对齐 prompt + 09-04）还是认软（改 prompt 放宽）。

## 3. prompt 面 — 根因已治 ✅（残留仅在可选风格节点）
- 活跃默认 prompt 已正确：`base-prompt.ts:10/12/136/174` 明写"好懂第一""极简/留白/克制不能当砍内容借口""严禁为压低逗句比剁碎句"，根因措辞已从默认强制清除。
- 残留"极简/克制/冷峻"全在**可选风格节点**（`node_description_style/opt_desc_minimal`、`node_tone/opt_tone_cold`、`node_dialogue_style/opt_dialogue_concise` 等），按角色/场景选，非默认强制，不回灌根因。
- **latent 风险**（非当前 bug）：若把上述冷峻极简节点设为强默认，可能偷回根因；当前默认 preset 正确，仅提示。

## 4. 语料面 — 已切热门网文 ✅
- `corpus/` = 斗破苍穹 / 全职高手 / 大奉打更人 / 完美世界 / 我在精神病院学斩神 / 斗罗大陆 / 灵境行者 / 第一序列 —— **全热门网文，无文学小说**，已对齐"好懂直白"决策。
- `finetune_data/train.jsonl` + `val.jsonl` 已存在（合成微调语料）。

## 5. 整改路径（按优先级）
1. **【待拍板·破折号政策】** 复硬 vs 认软 → 决定后改对应 detector + prompt，消除三轨漂移。
2. **【已完成·文档】** D2 + 破折号 两处打脸注释已改，`vitest 51/51` 仍绿。
3. **【建议】** 双树 not_shi 跨段/孤句模式源树未移植：若源树也参与验收则补移植；若仅 CLI 树验收则在注释固化现状（已注）。
4. **【后续】** 微调路线待授权（正版 txt + 装框架 + 算力）再动，不擅自跑。

## 6. 待你拍板
- 破折号：复硬（≥1 error）还是认软？
- push：网络通后是否推 `17d0f08` + `c65ba93`？
- 微调：是否启动（给授权）？
