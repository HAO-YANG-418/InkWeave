# .ts 改动精确 patch 提案（代码方执行 · 助手不碰源码）

> 配套：`网文可读性基准_覆盖晦涩极简偏差_2026-08-30.md` + `微调数据方案_热门网文语料_2026-08-30.md`。
> 目的：把「文学极简/克制/留白/低逗句比」的**数值硬推**从检测器 + 引擎层拆掉，与已降级的 `生成指令.md` / KB / `裂日.config.md` 对齐。
> 助手是方案方，以下只给精确 file:line + 当前文本 + 建议改法，**不直接改 .ts**。

---

## A. 检测器（数值逼碎句 → 降级为安全网）

### A1 `源码/checks/check-comma-chain.ts`

**L23（全局逗句比警告）** — 当前：
```ts
if (ratio > (thresholds.commaChainRatioWarn ?? 3.2)) {
  violations.push({
    ruleId: 'comma_chain',
    message: `逗号/句号比过高（${ratio.toFixed(1)}:1），存在"一逗到底"倾向`,
    severity: 'warning',
    suggestion: '在换拍、换判断、换压力处用句号断开，控制逗号/句号比在3.2以下',
  });
```
**建议**：阈值放宽 + 改措辞（不再硬卡 3.2）。`thresholds.commaChainRatioWarn ?? 5.0`，suggestion 改为「只在换拍/换镜头处断开；读着顺就保留逗号连写，不因数字硬拆句」。

**L38（单句逗号数 info）** — 当前：
```ts
if (commas >= (thresholds.commaChainLongMax ?? 8)) {
  violations.push({
    ruleId: 'comma_chain_long',
    message: `第${i + 1}句含${commas}个逗号，建议拆分`,
    severity: 'info',
  });
}
```
**建议**：`commaChainLongMax ?? 12`，message 改为「第N句含N个逗号，读着顺则保留，仅跨拍无换气点时拆分」。severity 保持 info（不废章）。

### A2 `源码/checks/check-sentence-waveform.ts`

**L30（句长标准差过低 warning）** — 当前：
```ts
message: `句长标准差过低（${stdDev.toFixed(1)}），节奏单调，建议长短句交替`,
severity: 'warning',
suggestion: '使用短句（≤10字）制造紧张感，长句（30+字）渲染氛围',
```
**建议**：保留节奏提示，但补「短句必须有身份（对话锋刃/动作感官链/危险发现），无信息量碎句反而伤可读」。

**L48（info）** — 当前 suggestion：`'在连续相似长度句子中插入短句或长句打破节奏'`
**建议**：改为「插入有信息量的短句/长句打破节奏；禁止为凑波形注入无身份碎句」。

---

## B. 引擎层（留白/含蓄类 → 直白优先，只停情绪层）

### B1 `源码/intent/engine.ts:345`
```ts
avoid: '不要让角色把话说完，留白比直白更有力'
```
→
```ts
avoid: '藏起因果与关键事实逼读者回读才懂；留白只停情绪层，叙事逻辑与谁做了什么必须写清'
```

### B2 `源码/reflection/engine.ts:1163`
```ts
suggestions.push('减少情绪词的直接使用，用含蓄的方式表达情绪')
```
→
```ts
suggestions.push('情绪落到具体身体反应/动作/微动作（含蓄只停情绪层，不藏事实与因果）')
```

### B3 `源码/knowledge/battle-styles.ts:458`
```ts
- ${style.type === 'artistic' ? '少写招式，多写气势——用环境变化代替动作描写，留白让读者脑补' : ''}
```
→
```ts
- ${style.type === 'artistic' ? '少写招式，多写气势——用环境变化强化氛围，但关键动作与结果必须写清，不藏逻辑' : ''}
```

### B4 `源码/writing/context-builder.ts:168`
```ts
if (style.tone < 0.3) parts.push('文风冷峻克制，避免过度抒情');
```
→
```ts
if (style.tone < 0.3) parts.push('文风冷峻克制（克制≠难懂，叙事逻辑必须写清），避免过度抒情');
```

### B5 `源码/kb/base-prompt.ts:109` 与 `源码/prompt-builder.ts:128`
```ts
- 形容词和副词克制使用，优先用精准动词和名词。能用"攥紧"就不用"紧紧握住"。
```
→ 追加一句：`读着顺优先；不为压低形容词数把句子剁碎（碎句比长句更伤可读）。`
（两处同步改）

### B6 `源码/kb/base-prompt-layered.ts:71-72`（铁则九描写克制）
```ts
export const REFERENCE_RULES = `【铁则九：描写克制】
- 同一段落内禁止连续2个以上"像/仿佛/如"比喻。直接写具体细节比打比方有力。
```
→ 追加：`克制是少堆修辞，不是把句子剁碎或藏逻辑；读者看不懂的克制=晦涩。`

---

## C. 预设（冷峻风 → 加好懂红线）

### C1 `源码/writing/presets/miner.ts:12`
```ts
description: '《我只是个矿工》风格：紧贴身体感知，冷峻克制，矿洞封闭感，壁温精确到小数点后一位',
```
→ 追加 `（克制≠难懂；数字量化不替代真实生理/动作描写）`。

### C2 `源码/writing/presets/miner.ts:22`
```ts
customInstructions: '叙事紧贴主角身体感知（温度、触感、震动、呼吸），通过身体感受传递恐惧/紧张/温暖。环境描写要有矿洞封闭感和压迫感。意象克制，避免华丽辞藻，用动词和名词而非形容词制造氛围。对话短，矿工说话不啰嗦。',
```
→ 末尾补：`叙事因果与关键事实必须写清，不靠"不说透"制造质感（好懂优先）。`

---

## D. 执行顺序与验证

1. 先改 A（检测器）— 让生成时不再被数值逼碎句。
2. 再改 B/C（引擎层 + 预设）— 去留白/含蓄类「藏逻辑」暗示。
3. `npm run build` 通过后，用 `inkweave check` 跑 ch58/60/62 验证：应仍有合理 warning，但**不再因逗句比/碎句把可读文判问题**；读 ch62 式「加料不碎」文应 0 error。
4. 配合微调（权重学「好懂」）两侧同步，避免 prompt 把模型拉回文学腔。

> 注：以上 file:line 为 2026-08-30 核身结果，执行前请代码方 `git diff` 复核当前行号（防分支漂移）。
