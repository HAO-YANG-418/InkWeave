# 回归验证 · 叙述口径（texture_variety）+ 密度分支删除（2026-08-29）

> 接方案方 `叙述口径mask复核_2026-08-29.md` §7 判决，由代码方落地 ① 密度分支删除 + ② texture_variety 改叙述口径，本文件记录实施与验证。
> 构建：`npm run build` → EXIT=0（含 compile-kb 重编译标定源）。

## 一、改动清单（真身）

| # | 文件 / 函数 | 改动 | 行号 |
|---|---|---|---|
| ①-a | `源码/checker.ts` R4 字数门禁 | 删除 `else if (totalWords < 0.9×目标)` 的密度判空分支（`sPerK<25 && aPerK<12`，因 `aPerK` 全样本 min 19.4 > 12 恒 false，是死代码）；只留 `< minChapterWords(2100)` 硬下限 → error。注释同步更新。 | 原 75–98 删除 |
| ①-b | `检测工具/checkers.ts` `checkWordCountTarget` 方案 B | 删除 `else if (ratio < 0.9)` 的密度豁免分支；`ratio<0.7`→error，`0.7–1.0`→统一走下方 `word_count_below` warning（不再判密度）。注释同步更新。 | 原 1209–1222 删除 |
| ② | `检测工具/checkers.ts` `checkTextureVariety` | 开头改用 `splitParagraphs(text).filter(p=>p.type==='narrative').map(p=>p.text).join('\n')` 作为 sourceText，剥对话；阈值 F=4/T=6 维持不变。 | 377–387 |

### 关键更正（代码方认错）
代码方上一轮称"`TextStats` 只有 `paragraphCount`、`narrative` 类型不存在"——**该结论对 CLI 树不成立**。
核验发现 CLI 树 `检测工具/checkers.ts`：
- `:30` `export type ParagraphType = 'narrative' | 'dialogue' | 'mixed'`
- `:51` `paragraphs: ParagraphInfo[]`（CLI 树 TextStats 带段落数组）
- `:102` `splitParagraphs()` 产出带 `.type` 的段落；`:289`/`:320`/`:825` 等多处已用 `stats.paragraphs.filter(p=>p.type==='narrative')`。

故 ② 直接复用 `splitParagraphs`（与 `:289` 同口径），无需改函数签名、无需新增数据结构。
源码树（`源码/checker.ts`）确实仍只有 `paragraphCount`、`computeTextStats` 仅 `split(/\n+/)`——但 ② texture_variety 在 CLI 树，不受影响。

## 二、验证（代码方实跑 `npx tsx _sandbox/profiler_dual_tree.mts 1 47`）

| 指标 | 预期 | 实测 | 判定 |
|---|---|---|---|
| `texture_variety` 触发章数 | 13（叙述口径标定，非15；ch28 新增属预期） | **13**（ch1/2/16/21/22/28/29/30/31/32/33/36/37） | ✅ |
| 全量 CLI error 合计 | 6（ch32–37 低于2100） | 6 | ✅ |
| 38–47 CLI error | 0（跑量批次零误伤） | 0 | ✅ |
| 源码树 error 合计 | 50（44+6，① 删死分支不影响 error 数） | 50 | ✅ |
| 构建 | EXIT=0 | 0 | ✅ |

### ② 生效判定
13 章与方案方在"叙述口径 F=4/T=6"标定分毫不差 → 叙述段过滤确实生效（非退化为全文）。
ch42 这类"对话里的疼"（6→1）已被排除，ch38 的"凉"不再计入对话段，符合 §7 判决 A。

### ① 生效判定
- ch32–37（<2100）仍走硬下限 → error，数量不变（6）。
- 删除的密度分支本就不产生 error（`isHollow` 恒 false），故源码 error 数维持 50。
- 2100–2700 区间旧"短而密放行 info"warning 消失，由 `word_count_below` warning 替代——均为非阻塞，不影响门禁。

## 三、遗留（按方案方 §7 判决，本期不动）

1. **「凉」走 U2 节点排除词表**：本期维持 F=4/T=6，不靠数值放宽。凉仍占 13 章触发中 7 章（54%），属"已知且合理"（克制残频武器高频是创作需要）。U2 节点排除词表是新机制，单独开一轮设计。
2. **密度判空彻底移除**：两树均已无密度判空分支，只留字数硬下限。旧"短而密放行"语义由 Y 降噪（全局 3→4）与 KB thresholds 承接，不再由写空门禁兼任。
3. **两树感官词典口径差**（CLI 五感/千字中位 40.9 vs 源码树 68，约 1.6 倍）：本期未动，仅影响密度豁免区（已删除），不影响 error 结论。

## 四、结论
①② 已落地、构建通过、回归全绿。CLI 树 `texture_variety` 进入叙述口径（13 章触发，对话误伤已消）；双树写空门禁统一为 2100 硬下限（密度分支死代码已清）。U2（凉节点排除）与两树口径统一留待后续，按铁律不空调、不盲扩。
