# D2 方案 B 切换指令（2026-08-22）

## 决策
用户拍板：D2「不是X是Y」监控口径从**方案 A（≥1处即error）**回切到**方案 B（≤2处warning / ≥3处error）**。

理由：第17–25章实战回归显示，9章共11处触发全部是正当破格（感官/认知辨识句式），方案 A 逼8/9章回炉代价过高，且与铁则三"有身份短句合法"逻辑冲突。方案 B 只卡≥3处堆砌的真 AI 味信号。

## 已完成的（方案方归我，只改文案不动 .ts）
- `InkWeave写作工坊/生成指令.md` 第13行：由"0容忍≥1处error"改为"≤2处warning / ≥3处error"。
- 同上文件第186行速查表：同步级别标注。

## 待代码方执行的（另一对话改 `检测工具/checkers.ts`）
定位 `检测工具/checkers.ts` 第734行：

```ts
// 现状（方案 A）
const severity = allMatches.length >= 1 ? 'error' : 'info';
```

改为（方案 B）：
```ts
// 方案 B：≤2处 warning，≥3处 error
const severity = allMatches.length >= 3 ? 'error' : (allMatches.length >= 1 ? 'warning' : 'info');
```

同时第732–733行注释 outdated，建议改为：
```ts
// D2 方案 B（用户拍板）：≤2处宽容（warning），≥3处才 error 作废。
// 1–2处为感官/认知辨识破格放行；仅卡≥3处排比式堆砌的真 AI 味。
```

## 回归预期（方案 B）
第17–25章：17/18/19/20/21/23/24/25 各 1–2 处 → warning，可过门禁；无章达≥3处。
唯一仍不过关：第22章（字数2151<2700，需补写至2700+）。

## 注意
- 本次契约正文已对齐，检测器 `.ts` 仍处方案 A，二者暂不一致——需代码方改完第734行后才完全对齐。
- 不改 `.ts` 的情况下，当前检测器仍按方案 A 跑（11处全 error）；切换生效以代码方落地为准。
