# 回归验证 · 铁则二十一（读者爽点本能）效果量化（2026-08-27）

> 验证目标：**#65 = 铁则二十一·读者爽点本能（生成指令.md:189）是否实打实涨**。用 ch29「弧心」作沙盒，before=原章（旧铁则），after=应用铁则二十一重写；跑双树门禁 before/after，数三项代理指标。
> 沙盒输出到 `InkWeave/_sandbox/ch29_after.md`，**不动原章节、不跑 `--save-fingerprint`**（不污染反向闭环指纹链）。

## 一、核真身（before 基线）
- 文件真身：`InkWeave/项目_裂日/章节/第29章 弧心.md`（79 行 ~2384 字）。
- 实测门禁（双树）：`❌ 未通过 | 评分 61/100（C级）| 🔴 0 错误 | 🟡 9 警告 | 🔵 3 提示`。
- 警告清单含 **`#7 对话缺乏碰撞`**、`#9 角色台词差异化`、`#2 读者负担`、`#3 字数偏少`、`#6/#8 开头缺少身体锚点`、`#5 句子过长`、`#1/#4 碎句/质感多样性` —— 正是读者视角报告点名的「主角工具人 + 对话像对讲机」短板。
- 章末：`朝着镇东，去掀母体藏最深的那个结` = **任务续集型**，无情绪钩。

## 二、沙盒 after（应用 铁则二十一）
- 重写标尺：铁则二十一四项（主角私心驱动 / 对话碰撞 / 设定危机呈现 / 章末情绪钩子），且**全程遵守其余 19 条铁则**（禁用字/破折号/比喻密度/连续对话）。
- 迭代记录（证明「铁则二十一 必须和基线铁则同时生效」）：
  - v1：D23（3 错误：禁用字×2省略号、紧邻重复、连续比喻过密 + 破折号×17、对话过载）—— 只补爽点、破基线 → **炸分**。
  - v2：D53（2 错误：紧邻重复「林深握紧方向盘」、连续比喻×2）。
  - v3：**0 错误，C73**。
- 实测门禁（双树）：`❌ 未通过 | 评分 73/100（C级）| 🔴 0 错误 | 🟡 6 警告 | 🔵 3 提示`。
- 警告清单：**已无 `对话缺乏碰撞` / `角色台词差异化` / `开头缺少身体锚点` / `句子过长`**（before 有），仅剩系统级 碎句/拍内断句/质感多样性/字数偏少（before 也有）。

## 三、三项代理指标（before → after）
| 代理指标 | before | after | 判定 |
|---|---|---|---|
| 对话碰撞次数（两方目的/信息差互顶） | 0（纯问答式传输） | ≥3（炸井vs保留标记；为个人赌全镇；救人为阿苓 vs 赵指责） | ✅ #65 填补 |
| 私心动机句数（主角行为由私心/恐惧/未了愿驱动） | 0（纯执行任务） | ≥4（「为阿苓…炸了永远找不着她」「为个人才赌得起」「救这人一半为阿苓」「那点甜是他仅剩的阿苓」+「他从来都是饵」框架） | ✅ #65 填补 |
| 章末钩子类型 | 任务续集（去镇东下一个点） | 情绪反转（「他从来都是饵」+ 母体引他回阿苓，猎人↔饵） | ✅ #65 填补 |

## 四、结论
1. **#65（铁则二十一）实打实涨**：双树门禁 61→73（同 C 级，+12），且**移除** before 的 `对话缺乏碰撞` / `角色台词差异化` / `开头缺少身体锚点` / `句子过长` 四项警告；三项代理指标从 0/0/任务续集 全部转为命中。读者爽点维度确被做进生成本能。
2. **关键约束**：铁则二十一不是「免死金牌」—— v1 只补爽点却破基线（破折号/禁用字/比喻密度/对话过载）直接 D23。结论：**读者爽点增益必须以「其余 19 铁则仍满分遵守」为前提**，这和写作基准「动笔即充实、不硬凑」是同一套本能。
3. **诚实短板（留作后续）**：after 字数 1479 < before 2384（我精简了部分过场）。两项均触发 `字数偏少（短而密，放行）`，门禁等价；但若要 after 与 before 等长对比，可把省掉的过场（下渠/泵房门）按写作基准补回，预计仍可保 0 error。这属于「写丰」层面，不属 #65 缺陷。

## 五、证据文件
- 沙盒 after：`InkWeave/_sandbox/ch29_after.md`（v3，0 error/C73）
- before 基线报告：`/tmp/before.txt`、`/tmp/after3.txt`（含完整门禁输出，可复核）
- 铁则二十一出处：`InkWeave/InkWeave写作工坊/生成指令.md:189`
