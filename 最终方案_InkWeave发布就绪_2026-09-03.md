# InkWeave 发布就绪 · 最终完整方案（2026-09-03）

> 一句话结论：**引擎架构没问题，问题在我一直撇开 skill 用"对话记忆"手写章节——这违反发布自包含原则。最终方案 = 把一切 craft 固化进 skill 的 KB（知识库/节点/预设/生成指令/base-prompt），标点与正文交给大模型生成、检测器兜底，出章改走 skill 三阶段流程，语料灌进去补"书库"。**

---

## 〇、我认的错（逐条对应你的四条）

| 你骂的 | 事实 | 我的错 |
|--------|------|--------|
| "大人只是举例，你得想办法系统解决" | 我把"大人。"当个例手改了一行，没系统性修 | 该把这类 prose 病固化成 KB 铁则/检测器规则，让所有用户、所有章自动规避，不是逐章手改 |
| "有些东西交给大模型更好，比如标点" | skill 设计本就是 LLM 生成 + 检测器门禁（base-prompt.ts:156-185 标点铁则 + checkers.ts 4 确定性规则） | 我绕开 skill 自己手写，标点反而比 LLM 差 |
| "skill做出来你不用？你想干嘛？" | 项目里 `InkWeave写作工坊/SKILL.md` 等三套发布级 skill 完备存在 | 我一直当"手写方"，没当"skill 执行方"，等于做了个 skill 不用 |
| "skill要发布，人家没对话记忆，你按记忆写是纯扯淡" | 生成指令.md 已含 21 条铁则（热门网文基准），但"双看反转 prose craft""都市爽文预设"等还只在我脑子里/对话里 | 致命错误：发布用户没有我们的对话，craft 必须进 KB，不能留在我记忆 |

**更正认知**：之前说"agent 手写不是大模型"不严谨——我本就用大模型 token 生成，但**撇开了 skill 的 KB 引导**，等于让模型裸写。正确做法是**用 skill 把 KB 注入模型上下文，模型按 KB 写，检测器门禁**。

---

## 一、核查事实（基于真实代码，非记忆）

| 项 | 现状 | 发布就绪？ |
|----|------|-----------|
| 标点规则 | `base-prompt.ts:156-185`【标点正确性铁则】+ `生成指令.md:4`"形式移交检测器"+ `checkers.ts` 4 确定性规则（question_period/tag_colon_mid/cjk_half_punct/repeat_qa）+ 破折号零容忍 | ✅ 已在 KB，归 LLM + 检测器兜底 |
| 内容铁则 | `生成指令.md` 铁则一~二十一（事件优先/读者理解链/外部压力/Show-Don't-Tell/身体锚点/开头轰炸/章末钩子/人味/内容密度/去AI味/爽点本能） | ✅ 已较全 |
| 知识库节点 | `知识库/节点/` 96 个节点文件（10 类） | ✅ 在 |
| 预设 | 10 个（玄幻修仙-起点/番茄、悬疑推理、无限流、末日废土、武侠江湖、游戏异界/竞技、灵异恐怖、历史架空） | ⚠️ **缺"都市爽文/系统流"** |
| 语料库 | `corpus/` 仅 `__selftest__` 空壳，**0 本 txt**；`scripts/finetune_prep.py` 完整存在但未跑 | ❌ **"书库不够"铁证** |
| 双看反转 prose craft | 大纲有伏笔规划（埋→触→提→收），但**写作工坊没教"正文怎么写埋线→后文回看恍然大悟"** | ❌ KB 缺位 |
| "X的。"收尾句式刷屏 | 许照 ch02-04 高达 9%/12%/13%，检测器未覆盖 | ⚠️ 需补 KB 禁令或检测器密度规则 |

---

## 二、最终完整方案（5 块，每块含 做什么 / 谁做 / 验收）

### A. 自包含化（发布前提：所有 craft 进 KB，不动我记忆）

- **A1 标点（已闭环）**：base-prompt.ts + 检测器已在位，确认不依赖对话。无需改。
- **A2 补"都市爽文"预设**：新建 `知识库/预设/都市爽文-系统流V3.1.md`（节点选择表：口语人味节点 / 爽点本能节点 / 短句连段节点 / 去冷峻节点），解决"套通用节点硬写"导致的铺垫反转弱。
- **A3 补"伏笔双看"铁则进 `生成指令.md`**（建议 铁则二十二·回看反转）：教正文如何埋线→后文回看恍然大悟。含 ✔✘ 示例：
  - ✔ 第一章闲笔"前合伙人老周来厂里借过一次公章"；第八章顾成说"吞厂的是老周"→读者回看第一章瞬间懂。
  - ✘ 阴谋在揭晓章凭空冒出，前面零铺垫（当前都市爽文章的毛病）。
- **A4 固化本轮你点名但只在对话里的 craft**：去"X的。"刷屏（加 KB 禁令 + 可选检测器 `de_ending_density` 密度规则）、开头身体反应、对话碰撞——全部写进 KB，删掉我脑子里的版本。

### B. 标点归 LLM（你明确要的）

- **生成指令.md 已写明"形式约束移交检测器"**——标点由 LLM 自然处理（模型训练足，这正是你说的"交给大模型"），检测器只拦 4 确定性硬伤 + 破折号零容忍 + 半角/引号错位。
- **我不再逐章手改标点**。今后标点问题只在两种情况下处理：① 检测器报 error → 走 `--fix` 自动修；② 发现新标点硬伤类 → 加进 KB/检测器（惠及所有用户），不当章手改。
- **key 问题说死**：标点归 LLM 是 skill 架构内、零 key。key 只在两处出现——(a) 发布后**终端用户自带**底座配置（CUSTOM_BASE_URL+MODEL，用户自己的）；(b) 微调底座（C2，你拍板）。我不向你要 key。

### C. 语料 / 书库（"书库不够"根因）

- **现状**：corpus/ 0 本，finetune_prep.py 完整但未跑。
- **路径（你提供 8 本授权 txt 后）**：
  - **C1（推荐·零成本·立即可用）**：`finetune_prep.py` 扒→洗→切→过滤→jsonl → 从 jsonl **蒸馏"铺垫→反转 / 口语人味 / 感官变奏"范例**进 `生成指令.md` + 节点。LLM 按范例写，不微调也能涨。这直接回答"不会就去学习"——学从语料来，不从我猜。
  - **C2（治本·需你拍板框架算力）**：微调底座，把 craft 烤进模型分布（治"整体好懂直白"这种分布级问题）。与 C1 不冲突，C1 先上。
- **8 本定稿（之前已定）**：斩神/斗罗/完美世界（你指定）+ 大奉打更人/斗破苍穹/全职高手/灵境行者/第一序列（调研选定，口语化好懂）。**禁用**诡秘之主（维多利亚古典腔）、捞尸人（民俗黑话）。

### D. 出章改用 skill 驱动（你"skill做出来你不用？"）

- **今后所有出章走 skill 三阶段**：`pre-analysis`（写前分析）→ 生成（LLM 按 KB 写）→ `check`（检测器 0 error 门禁）。我当 skill 的**执行方/校验方**，不再当手写方。
- **验证自包含**：在一个**清空对话**里只靠 skill 跑出一章，证明不依赖我们的记忆。这是发布前的硬门槛。

### E. 发布门禁（打包前必须过）

1. `npx tsx 检测工具/verify-gates.ts` 五维门禁全 ✓；
2. `npx tsx 检测工具/check-all.ts` 全卷 0 error、破折号 0；
3. 清空对话自包含测试通过（D）；
4. 8 本语料 craft 已蒸馏（C1）或标注"待语料"（C2 未启动时）；
5. A2/A3/A4 全部落 KB。

---

## 三、里程碑排期（建议）

| 阶段 | 内容 | 成本 | 依赖 |
|------|------|------|------|
| **本周·立即可做** | A2 都市爽文预设 + A3 双看铁则 + A4 固化 craft；用 skill 重出都市爽文示范章证明铺垫反转可补 | 零成本 | 无 |
| **语料就位后** | C1 蒸馏 8 本 craft 进 KB | 零成本（你提供 txt） | 你授权 8 本 |
| **你拍板后** | C2 微调底座（框架/算力你定） | 需算力 | 你决策 |
| **发布前** | E 五项门禁 | 零成本 | 上述完成 |

---

## 四、我下一步立即执行（等你确认"动手"）

1. 建 `知识库/预设/都市爽文-系统流V3.1.md`（A2）；
2. 给 `生成指令.md` 加 铁则二十二·回看反转 + 去"X的。"禁令（A3/A4）；
3. 用 skill 三阶段重出一篇**都市爽文示范章**（D），act1 埋 2-3 个伏笔、揭晓章形成真"双看"，给你看铺垫反转长啥样、且 0 error。

完成即证明：skill 自包含、标点归 LLM、铺垫反转可补——三条一次性闭环。

---

## 五、验收标准（方案算成功）

- [ ] 清空对话里只靠 skill 跑出一章 0 error（自包含）；
- [ ] 标点 error 由检测器 `--fix` 自动处理，无我手改；
- [ ] 都市爽文章有可识别的"埋线→回看恍然大悟"双看结构；
- [ ] corpus/ 有 ≥1 本语料跑通 finetune_prep → craft 蒸馏进 KB；
- [ ] verify-gates + check-all 全过。
