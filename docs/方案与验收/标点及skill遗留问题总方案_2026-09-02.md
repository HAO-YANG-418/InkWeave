# 标点及 skill 遗留问题整改总方案

- 日期：2026-09-02
- 定位：整合 `09-01 标点学习闭环` + `09-02 破折号硬禁整改` + 全部未解决问题，给出**待拍板清单**。
- 原则：已执行的只列结论（附 file:line 证实），未解决的给方案 + 拍板点，不擅改代码。

---

## 〇、当前状态速览

| 维度 | 状态 |
|------|------|
| 标点规范文档 | ✅ 已落 `docs/方案与验收/标点规范_网文写作_2026-09-01.md` |
| 生成端铁则 | ✅ `源码/kb/base-prompt.ts:118` 标点正确性铁则 |
| 检测器 4 规则 | ✅ `检测工具/checkers.ts:236-239` question_period / tag_colon_mid / cjk_half_punct / repeat_qa |
| 破折号全树硬禁（P0） | ✅ 已执行（见下） |
| 双树默认语气对齐（P1） | ✅ 已执行 |
| 冷节点 5 倍矛盾（P2） | ✅ 已并入 P0 修正 |
| 四章现行出货 | ✅ 全 0 错误、破折号全书 0（check-all 实跑） |
| 备份 | ✅ `punct_backup_2026-09-02/` 7 文件字节一致 |

**待解决（skill 构成相关）**：漏引号/错位、引号内点号缺口、顿号缺口、双树审计；legacy 草稿已归档✅。与 skill 无关（扩章/微调）见第三章，不整。

---

## 一、已闭环明细（证实非空谈）

### ✓ P0 破折号硬禁（用户拍板"先备份再整"后执行）
- `检测工具/checkers.ts:381-393`：删"每章≤2 破格额度"，改 **1 个即 🔴 error**；`:368` 注释同步。
- `源码/kb/presets/preset_base_webnovel.json:80`：`no_dash` `warning` → **`error`**。
- `源码/kb/base-prompt.ts:126`：标点铁则补"破折号全局禁止（零容忍）"。
- `opt_rhythm_short:26` / `opt_rhythm_medium:28`："少用 / 避免过多" → **禁止破折号**。
- `opt_tone_cold:21`：删"破折号谨慎使用"，并把"千分之五" → **千分之一**（消 5 倍矛盾）。
- `opt_tone_playful:20`：删"可以用括号、破折号补充内心吐槽"。

### ✓ P1 双树对齐
- `preset_base_webnovel.json:14`：`node_tone` 由 `opt_tone_cold` → **`opt_tone_colloquial`**（与运行时默认一致，消除检测树默认用冷语气的陷阱）。

### ✓ P2 清理
- 冷节点感叹号 5 倍矛盾已并入 P0 的 `cold:21` 修正。
- **引号内点号 / 顿号轻量检测主动搁置**（正则误杀风险高，会冲垮 4 章 0 错状态）。

### ✓ 验证
- `check-all 实战出文` → 4 现行章（ch01_v3 / 02 / 03 / 04）**全 0 错误**，零回归。
- 脏探针（含 3 破折号）→ 🔴 禁止破折号 error 触发，硬禁生效。
- 回滚路径：`cp punct_backup_2026-09-02/<file> <原路径>`。

---

## 二、skill 构成相关·待解决（方案 + 拍板点）

> 本章只列**构成 skill 本身**的待办（提示词规则 / 检测器 / 节点一致性）。出文、软件化等不在 scope。

### ① 漏引号 / 引号错位（语义层硬伤）— 严重度 🟡
- **现状**：确定性检查只数"引号成对数"，漏一个或错一个总数仍可能平衡，机器查不出。属语义层，已写进规范文档第 10/11 节通读自检清单。
- **方案**：
  - 短期（立刻）：盲写后强制**通读逐句核对**——判断"这句到底是不是人物自己说的"，台词绝不能写成叙述（漏引号），提示语不能包进引号（错位）。
  - 中期（可选）：探索"引号语义一致性"轻量检测，误杀风险高，不急。
- **拍板点**：接受"靠通读兜底"，还是投入做语义级检测？

### ② 2 个 legacy 草稿带旧半角引号错 — → ✅ 已闭环（A 归档，2026-09-02）
- 原状 / 执行 / 验证见上轮记录：`mv` 至 `InkWeave/legacy_drafts/`，`check-all 实战出文` 现 4 章全 0 错误（2A/1B/1C，8546 字）。留底可恢复。

### ③ 引号内点号位置检测缺口 — 严重度 🟢
- **现状**：引文独立成句→点号在内；作句子成分→引号内无句末点号（问号/叹号保留）。目前不查。
- **方案**：暂靠通读；可选窄检测（评估误杀）。
- **拍板点**：是否加窄检测？

### ④ 顿号并列检测缺口（低风险）— 严重度 🟢
- **现状**：并列用顿号不查（低风险）。
- **方案**：暂搁置，靠通读。

### ⑦（可选）双树全量一致性专项审计 — 严重度 🟢
- **现状**：本次只修 `node_tone` 默认不一致，未全扫"生成树 vs 检测树 preset"的所有差异。
- **方案**：专项 grep 比对两树语气/节奏/禁用字符口径。
- **拍板点**：是否现在做？

---

## 三、与 skill 无关 · 不在本次 scope（按你红线不整）

### ⑤ 章节数太少 / 扩章 — 严重度 🟡
- **扩章是什么**：续写 ch05+ 把老周 / 姓孙线收束成更多章节。本质是**用 skill 出货（应用）**，不是 skill 构建本身。
- **红线**：你"skill 做好再说"= skill 未拍板前不扩章；且扩章非 skill 构成，按"跟 skill 无关的不要整"**不碰**。
- **触发**：skill 标点达标、你主动说要扩章才启动。

### ⑥ 微调根治路线（长期）— 严重度 🔵
- **现状**：`skill 烂 → 走微调` 已定（语料 = 8 本热门网文好懂流）。
- **红线**：属软件化自动批量，须你主动提才碰（铁律），与 skill 构成无关，**不整**。

---

## 四、建议执行顺序（仅 skill 构成内·待拍板）

1. **问题① / ③ / ④**（质量天花板：通读兜底 vs 加窄检测）
2. **问题⑦**（可选双树审计）

> ⑤ 扩章 / ⑥ 微调不在顺序内（与 skill 无关）。

---

## 五、验证标准（每次改动后）

- `npx tsx 检测工具/check-all.ts "实战出文"` 实跑，现行章保持 0 错误。
- 脏探针验证新规则触发（破折号 / 疑问用句号 / 中文后半角等）。
- 盲写后**通读逐句核对**（覆盖语义层漏引号/错位）。
- 改前先备份（`punct_backup_2026-09-02/` 机制可复用）。
