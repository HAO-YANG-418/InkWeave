# 全面审计 · 未完成的与不足（2026-09-02）

> 方法：实时跑 `check-all 实战出文` 拿活数据 + 逐文件读真代码核对（非凭记忆/先验）。
> 结论分级：✅ 已闭环·经核实 / 🔴 真问题（会误导或阻断）/ ⚠️ 不足（不阻断但需补）/ 🟡 已知缺口（文档标过未动）。

---

## 一、✅ 已闭环、且本次实核不倒退的能力

| 能力 | 真代码证据 | 状态 |
|------|-----------|------|
| 破折号硬禁（1个即 error） | `检测工具/checkers.ts:381-393` severity=error | ✅ |
| 全角弯引号双树可见 | `检测工具/checkers.ts:93/98/139/1090/1212/1552` + `源码/checker.ts:331/573/680/998/1049/1291/1735` 均含 `[“”「」『』‘’]`（U+201C/U+201D） | ✅ |
| dist 同步 | `dist/源码/kb/base-prompt.js:12/55/95-97` 含「口语人味/性格驱动/好懂直白」补丁串 | ✅ |
| 节点默认=口语人味（**推翻"节点仍冷峻"假设）** | `node-registry.ts` 默认：tone=`opt_tone_colloquial`(L110)、dialogue=`opt_dialogue_natural`(L55)、rhetoric=`opt_rhetoric_plain`(L186)、emotion=`opt_emotion_both`(L97)、desc=`opt_desc_sensory`(L69)——**均非极简/冷峻/留白默认** | ✅ |
| 全章 0 error | `check-all 实战出文` 实时：5章全 0错误，平均87.8，破折号总数0 | ✅ |

> 注：节点文件里 `极简/克制/留白/冷峻/文学性` 字样仍在（opt_desc_minimal、opt_dialogue_concise、opt_emotion_show、opt_rhetoric_sketch 等 12+ 节点），但那是**非默认选项的描述**，不是默认输出，不矛盾。

---

## 二、🔴 真问题（最该修，会误导判断）

### 🔴1. 门禁双路径结论打架（最危险）
- `检测工具/check-all.ts:438` 只调 `checkChapter(texts[i])`——**仅 CLI 树（检测工具/checkers.ts）**。
- `检测工具/check-chapter.ts`（单章）额外跑 `源码/checker.ts` 的 `check`（R1 双跑聚合），含 `连续对话过多`（`源码/checker.ts:597/613`）、`角色台词差异化` 等。
- **同文件 ch5 实测**：check-all = **99分 / 0警告**；单章 = **85分 / 3警告**（连续对话过多×2 + 角色台词差异化）。
- 后果：「0错误可出货」结论**取决于走哪条命令**，会误判为全绿。
- 建议：以单章 `check-chapter.ts`（含 R1）为权威门禁，并把 `源码/checker.ts` 的 R1 检查并入 `check-all`，二选一统一，否则永远两套标准。

### 🔴2. 默认字数节点(2200) 与 门禁(3000) 脱节
- `node-registry.ts:239` 默认 `opt_length_2200`；盲写门禁 `--target 3000`。
- 引擎自带默认产出比门禁短约 1/3，盲写默认章必触发「字数不足」warning（ch01-04 原 ~2100 即此因，靠手扩补到 3000+）。
- 不修则"盲写即达标"永远做不到，每章都得二次扩写。
- 建议：`opt_length_2200` → 提到 3000（或新增 opt_length_3000 并设为默认），与门禁对齐。

---

## 三、⚠️ 不足（不阻断出货，但拉低质量/观感）

### ⚠️3. 连章开头章 ch01 最弱（78分 C级 / 5警告）
`check-all` 实时指出 ch01_v3：
- 不是X是Y ×1（warning；按 D2 方案B `checkers.ts:939` ≤2 处放行，设计如此，但仍是 AI 味残留）
- 碎句：连续3句≤8字无动作/感官信息
- 排比堆叠：「听见」×4（同句式≥3 机械感）
- 对话引导词重复：角色"国庆" 说/道/问/答 ×6（超每角色每章3次）
- 作为"连章出货"的**开头章**最弱，直接拉低整书观感。

### ⚠️4. 检测器自身两处互相打架（info 级，不阻断但丢可信度）
- **数据锚点自相矛盾**：同章既报「过高 14.4/千字」又报「过低 0.0/千字」（`检测工具/checkers.ts:743` + `:746`，两分支同弹，互斥却共存）。
- **角色台词差异化误算**：报「对话占比49%」与基础统计「对话比例1.2% / 叙事54段对话4段」矛盾；且它数的是**对话标签类型**非**声口差异**（年轻人/哑嗓声口分明不同仍报 0 种，`checkers.ts:772`）。属检测器侧 quirk，非写作缺陷。

### ⚠️5. 连章出货体量薄
- 实产 5 章 = 都市_许照 4 章（同书连章）+ 矿工 ch5（独立 demo）。
- 连章素材仅 4 章一本，作为卖课宣传样本偏少；且开口章 ch01 最弱（见 ⚠️3）。
- 用户要"连章出货 battle 热门网文"，当前样本量不足以展示一本书的连贯质量。

---

## 四、🟡 已知缺口（文档标过，未动，非本轮范围）

- 漏引号 / 引号错位：语义层，确定性检测不覆盖，靠通读逐句兜底。
- 引号内点号、顿号缺口：顿号已主动搁置（正则误杀风险高）。
- 双树全量审计：未做（生成树 vs 检测树 config 一致性逐项核）。
- 微调根治：未启动（语料/框架/算力待拍板，本阶段不需 key，属软件化阶段）。

---

## 五、总体判断

- **核心能力真落地、不倒退**：口语人味范式 + 0错门禁 + 破折号硬禁 + 引号可见 + 节点默认口语化，五项均经实时/真代码核实。
- **但"出货闭环"有两道裂缝会误导人**：① 门禁双路径不一致（最危险，能让 85分章被报成 99分）；② 默认字数节点与门禁脱节（盲写永远差 700 字）。
- **检测器 2 处自相矛盾**丢可信度，但不阻断出货。
- **最该补的"不足"**：连章体量 + 开口章(ch01)质量。

## 六、建议执行顺序（待拍板，不擅改）
1. 统一门禁：把 `源码/checker.ts` R1 检查并入 `check-all`，或明文规定单章 `check-chapter.ts` 为权威（🔴1）。
2. `opt_length_2200` → 3000 对齐门禁（🔴2）。
3. 补强 ch01（不是X是Y / 碎句 / 排比 / 引导词）（⚠️3）。
4. 修检测器两处矛盾（数据锚点互斥、台词差异化占比误算）（⚠️4）。
5. 扩连章体量（同书再出 2-3 章）（⚠️5）。
