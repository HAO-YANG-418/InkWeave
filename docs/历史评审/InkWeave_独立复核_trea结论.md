# InkWeave 独立复核：trea 结论一致性核查（2026-08-17）

> 定位：用户已接收 trea 的“确认一致”结论，要求 WorkBuddy 独立核实一致性并给下一步建议。
> 方法：逐条读真实源码/指纹/生成指令，不盲信任何一方的“done”。

## 一、trea 结论里“确认到位”的三项——实测核对

| trea 声称 | 实测结果 | 是否真 |
|---|---|---|
| `assertGate` 让 error 退出码 1 | `check-chapter.ts:27-32` 确为函数 + `process.exit(1)`；`:137/:186` 两处调用 | ✅ 真 |
| 检测器/生成指令/版本号统一 v4.8 | 检测器 `checkers.ts:405` 用 75%；生成指令 `〇.七` 写 75%；四 SKILL + VERSION 全 v4.8 | ✅ 表面一致 |
| 生成指令加了“〇.七 写后门禁” | `生成指令.md:53-63` 确有，且写明 error=重写、指纹注入下一章 | ✅ 真 |

**结论**：三项核心修复确实落地，非口头。

## 二、trea 没意识到的两个真问题（重点）

### 问题 1：「阈值打架」比 trea 描述的更严重——同一个文件里自己打自己
trea 摘要表述为“检测器 75% vs 写作规则 45%”。实测**三条标准全不一致**：
- 铁则八（`生成指令.md:103`）：视觉占比 **≤45%**
- 〇.七 门禁（`生成指令.md:60`）：error 判定写“视觉占比 **>75%**”
- 检测器（`checkers.ts:405`）：`visualPct > 0.75 ? error : warning`

→ 46%~74% 视觉的章节能过门禁/检测器，却违反铁则八。
→ **根因**：WorkBuddy 加 〇.七 时随手抄了检测器的 75%，没对齐铁则八。这是“修复动作引入的新不一致”，trea 没点出。

### 问题 2：`--fix` 自动修复救不了排比堆叠（trea 未验）
- `autoFix`（`checkers.ts:1258-1285`）**只做破折号→逗号**一种确定性替换。
- 第1277-1278行明确注释：连续短句合并“属于语义相关修复，跳过”；**排比堆叠同理不在 autoFix 范围内**。
- 实测推论：第14章 16 组排比，`--fix` 跑完仍是 16 组 → 门禁照样卡死。
- trea 摘要里写“检测+自动修复”易被理解成“能自动消排比”，**实际不能**。风格类 error 只能人重写或 LLM 重写。

## 三、已确认的事实数据
- 第14章指纹：`sense_density_balance`（视觉 83%，89/107）+ `style_stacking_verb`（16组排比）→ 2 个 error，退出码 1。证明门禁真生效、且旧章节确实普遍踩铁则。
- 全卷 15 章种子指纹已铺满（含 stylePatterns），闭环数据层就绪。

## 四、下一步建议（按性价比）
1. **【高·必修】对齐感官阈值到铁则八（45%）**：改 `checkers.ts:405`（`>0.45` 即 error，删掉 0.75 分支）与 `生成指令.md:60`（门禁描述从 75% 改为 45%）。否则新章节继续产“过门禁但违规”的半成品。
2. **【中】明示 `--fix` 边界**：在 `生成指令.md` 〇.七 或 help 里写清“`--fix` 仅修破折号，排比/感官需重写”。避免用户误以为一键能过门禁。
3. **【中】`autoFix` 是否要补排比改写**：若要让门禁可自动收敛，需给 `autoFix` 加“保留1处排比、其余换句型”的语义级改写（风险较高，建议人工/LLM 做，不在自动修复里硬上）。
4. **【低·待办】效果度量**：仍无盲评/A-B 数据，变现硬伤未动。

## 五、本次未改动代码（遵循“找茬/核查”定位）
仅做只读核实 + 本文件记录。待用户确认方向再动手。
