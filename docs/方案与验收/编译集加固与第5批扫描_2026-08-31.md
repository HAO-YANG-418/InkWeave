# 编译集加固 + 第5批种子扫描（2026-08-31 22:50）

> 起因：上一轮收尾留的最后一个待办——`源码/node-registry.ts` 靠 `prompt-builder.ts:14` 的 import **传递编译**才进 dist，import 一断就静默失效。
> 本轮按"举一反三"扩大到全树核查，结果不止一个文件。

---

## 一、举一反三：本会话改过的 .ts 逐个核 include

| 文件 | 原状态 |
|---|---|
| `源码/node-registry.ts` | ❌ 不在 include（已知） |
| `源码/knowledge/creative-leaps.ts` | ❌ **不在 include（本轮新发现）** |
| `源码/knowledge/battle-styles.ts` | ✓ |
| `源码/cooling/creative-diverge.ts` | ✓ |
| `源码/writing/presets/wasteland.ts` | ✓ |
| `源码/intent/engine.ts` | ✓ |
| `源码/writing/context-builder.ts` | ✓ |
| `源码/writing/fingerprint-inject.ts` | ✓ |
| `源码/kb/base-prompt.ts` | ✓ |
| `源码/prompt-builder.ts` | ✓ |

`creative-leaps.ts` 上轮改的措辞**确实进了 dist**（22:29 产物已含新文案），但那是靠 `knowledge/index.ts` 链的传递编译——与 `node-registry.ts` 是完全同一种脆弱。

## 二、更大的发现：整批文件完全不在编译集

用 `npx tsc --noEmit --listFiles` 与目录清单对比：

| 目录 | 不在编译集的文件 |
|---|---|
| `源码/writing/presets/` | game-isekai / infinite-flow / light-novel / wuxia / composite-preset / index（6 个） |
| `源码/writing/` | agent / engine / index / learning-bridge / **orchestrator** |
| `源码/cooling/` | index / local-storage-storage / param-filler / types |

`writing/presets/` 这批最要紧——上轮正是在 `wasteland.ts` 的 `customInstructions` 里抓到"冷峻克制"种子。同类载体有 5 个从没进过编译集，改了也不会同步。

## 三、第5批种子扫描：结论是干净的

对 5 个题材预设 + orchestrator/agent/engine 按 5 概念（逼碎句 / 逼冷峻白描 / 硬计数 / 留白藏逻辑 / 禁止口语人味）全扫：

- **5 个题材预设零命中** —— 无冷峻/留白/碎句种子，第5批不存在。
- 唯一命中：`源码/writing/orchestrator.ts:172`
  ```
  sensory_richness.target: '增加感官描写密度，每300字至少切换一次感官类型，禁止连续视觉描写超过3段'
  ```
  **判定：故意不动。** 它是 `reviseTemplates` 的整改模板（检测到感官不足时才触发），方向是**补感官**，正好修 A7 五感短板，与"逼冷峻/逼碎句"完全反向。这里的硬数是整改目标，不是有害种子。
  → 记入长期笔记，防下轮误清。

## 四、加固落地

`tsconfig.json` include 12 → 19 项，新增 7 个文件：

```
源码/node-registry.ts
源码/knowledge/creative-leaps.ts
源码/writing/presets/game-isekai.ts
源码/writing/presets/infinite-flow.ts
源码/writing/presets/light-novel.ts
源码/writing/presets/wuxia.ts
源码/writing/presets/composite-preset.ts
```

**`源码/writing/orchestrator.ts` 加不进去**（试过，TSC_EXIT=2）：它传递依赖两个腐坏文件——

```
源码/writing/engine.ts   → 缺 ../technique/reader-model、../technique/library、
                             ../technique/types、../anti-pattern/chapter-type-tracker、
                             ../anti-pattern/conflict-diversity-check、../anti-pattern/template-composer
源码/kb-loader.ts:176    → 缺 ./kb/presets/preset_miner.json
```

要纳入 orchestrator，须先补这些缺失模块 → 记为 P3 工程债。

## 五、验证（端到端，不靠 EXIT=0 自证）

| 项 | 结果 |
|---|---|
| `npm run build` | **EXIT=0**，compile-kb 95 节点 |
| `tsc --noEmit --listFiles` 编译集 | 295 → **305**，7 个新增文件全部在集 |
| dist 产物 | `node-registry.js` / `creative-leaps.js` / `wuxia.js` / `light-novel.js` 均 **22:50 新产物** |
| dist 默认值补丁串 | `opt_rhythm_mixed` / `opt_emotion_both` / `opt_tone_colloquial` 三处命中；旧默认 **零残留** |
| 端到端探针（实跑 `getDefaultSelectionsFull()`） | **PROBE_RESULT=PASS** —— mixed / both / colloquial / sensory / standard 全对（探针用后已删） |
| `verify-gates.ts` 回归 | **5/5 门禁通过** |

## 六、状态

| 项 | 状态 |
|---|---|
| 运行时默认底座去冷峻 | ✅ 探针实证 |
| 逼碎句硬约束清零（15 处） | ✅ |
| 第4批白描/硬计数/留白种子清零（15 处） | ✅ |
| 第5批扫描 | ✅ 干净，无需清 |
| 编译集加固 | ✅ 305 文件，7 项补入 |
| 脚手架剥离 + 四章盲写不塌 | ✅ v3/v4/v5/v6 |
| A11 人味·自嘲 | ⏳ 唯一剩余真短板，待带底座盲写验证 |
| orchestrator/engine 纳入编译集 | ⏳ P3 工程债（需补 technique/anti-pattern 模块） |
