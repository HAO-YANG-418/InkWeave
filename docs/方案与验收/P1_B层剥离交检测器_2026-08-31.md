# P1 · B 层 10 项形式约束剥离交检测器（2026-08-31 23:xx）

## 前提核真身（关键：推翻"检测器已全覆盖"旧假设）

P1 原本的预设是"检测器已全覆盖 B 层 10 项，剥了零风险"。**实查两棵树后发现该假设不成立**：

| B 项 | 形式约束 | 源码/checks/ | 检测工具/checkers.ts | 结论 |
|------|---------|-------------|---------------------|------|
| B1 破折号 | `——` 禁用 | check-forbidden-char | forbidden_char_dash (L381) | ✅ 覆盖 |
| B2 一逗到底 | 多镜头逗号串句 | check-comma-chain | comma_chain (L318) | ✅ 覆盖 |
| B3 碎句身份 | 无身份碎句 | check-sentence-waveform | short_sentence + sentence_waveform (L252/255) | ✅ 覆盖 |
| B4 引导词 | 同角色每章≤3次"说" | — | checkCharacterVoice 仅查"标签种类≥3"弱代理 (L661) | ❌ **缺口** |
| B5 段落开头 | 连续3段同主角名 | — | 段落开头重复 (L1068) | ✅ 覆盖 |
| B6 排比 | 排比堆叠 | — | style_stacking (L262) | ✅ 覆盖 |
| B7 "的"去重 | X的Y堆砌 | — | deDensity>0.08 (L1056) | ✅ 覆盖 |
| B8 字数 | 目标±10% | text-stats | word_count_hard_error/below/long (L1212+) | ✅ 覆盖 |
| B9 套路词 | 万能套路词 | — | cliche (L186-195) | ✅ 覆盖 |
| B10 不是X是Y | 不是X是Y句式 | — | not_shi_pattern (L254) | ✅ 覆盖 |

**结论**：9/10 全覆盖，**仅 B4 引导词零覆盖**。若直接剥铁则十四会变成"零风险"假象、制造回归。

## 修复：先补 B4 检查（让 P1 真正零风险）

- `检测工具/checkers.ts` 新增 `checkDialogueTagRepeat` + 注册 `{ id:'dialogue_tag_repeat', name:'对话引导词重复' }`（warning 级，对齐铁则十四"软指导"定位）。
- 逻辑：正则 `([一-龥]{2,4})(说|道|问|答|喊|叫)` 统计每角色名引导词次数，>3 报 warning，建议轮换。2–4 字名规避"他/你/听"等代词误判。
- 端到端验证（绝对路径跑 `inkweave check`）：
  - 探针A（林深说×5）→ `### 警告 #3：对话引导词重复` ✅ 触发
  - 探针B（干净，引导词轮换）→ 无触发 ✅ 无误报
  - 探针A 同章还有 警告#1/#2 → 既有检查管线照常运行，无崩溃/无回归。

## 剥离：重写 生成指令.md（v4.8 → v4.9）

- **顶部新增说明**：10 项形式约束已全部移交检测器（写后门禁），本章不再重复列规则，省下 prompt 带宽给内容能力。
- **移除的 B 层**：写前自检#1(破折号)/#2(不是X是Y)/#3(句式)/#4(字数)；铁则三(句群节奏)/四(一拍一息)/十二(破折号)/十三(句式控制)/十四(引导词)/十五(段落开头)/十六(排比)/十七(的的去重)；技法二(不是X是Y)/三(破折号)/四(拍内融合)/五(碎句免疫)；〇.六 排比/的-去重段；禁令中的形式项；通用套路词禁用。
- **保留的 A 层内容能力**：铁则一/二/五/六/七/八/九/十/十一(字数标注检测器核查)/十八/十九/二十/二十一；技法一(对话融合)；镜头链；收束模式；内容质量禁令；写后门禁；〇.五节奏预热。
- 铁则十一 的字数/场景数/对话占比标注"由检测器核查"（对应 word_count_*/scene_count/dialogue_overload，确已覆盖）。

## 验证
- `npm run build` EXIT=0（编译 源码/ + 检测工具/，含新函数）。
- 两探针端到端：B4 新检查触发且无误报，既有检查无回归。

## 收益
- prompt 带宽释放：生成指令.md 从 257 行 → 约 150 行，B 层硬规则全删，LLM 生成时不再被形式清单分心。
- 形式质量仍由检测器硬兜底（含新补的 B4），零风险。

## 待办状态
- P1 完成。剩余 P2（A层15项评分卡固化）/ P3（编译集工程债：补 technique/anti-pattern/preset_miner 模块）。
