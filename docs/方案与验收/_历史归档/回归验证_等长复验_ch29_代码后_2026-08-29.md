# 回归验证 · 等长复验 ch29 · 代码后重跑（2026-08-29）

> 本文件是 `回归验证_等长复验_ch29_2026-08-27.md` 的**代码后回归补章**。
> 目的：8-27 等长复验报告基于当时检测代码；8-29 代码方对检测树做了三处实改（① 删密度死分支、② texture_variety 改叙述口径、③ R4 字数门禁方案B）。须独立核真身确认 #65（铁则二十一·读者爽点本能）的等长结论在代码变更后仍成立、且未引入门禁回归。

## 一、方法

- 当前代码核真身（`检测工具/checkers.ts`）：
  - ② `checkTextureVariety`（:377-385）改用 `splitParagraphs(text).filter(p => p.type === 'narrative')` 作 sourceText（叙述口径，与 `:289` checkCommaChain 同口径）。✅ 落地。
  - ① `checkWordCountTarget`（:1201-1216）**只留 `ratio < 0.7` 硬下限 → `word_count_hollow` error**；原 `0.7–0.9` 密度豁免分支已删除。✅ 落地。
  - ③ R4 方案B（源码树 `源码/checker.ts` 同步，本报告不重跑源码树，仅确认 CLI 树口径）。
- 验证命令（沿用 8-27 协议，不加 `--save-fingerprint` 不污染指纹链）：
  - `npx tsx 检测工具/check-chapter.ts "<abs>" --target 3000`
  - 沙盒：`InkWeave/_sandbox/ch29_after_v2.md`（v2.5，2238 字，无引号对话）
  - 原章：`InkWeave/项目_裂日/章节/第29章 弧心.md`（before，2384 字，引号对话）
- 对比标尺：8-27 报告 `回归验证_等长复验_ch29_2026-08-27.md` 的 claim。

## 二、当前代码实测结果

| 维度 | before（原 ch29） | after（ch29_after_v2 等长版） |
|---|---|---|
| 门禁结果 | ✅ 0 error（通过） | ✅ 0 error（通过） |
| 实际字数 / 完成率 | 2384 / 79% | 2238 / 75% |
| 段落分类 | 叙事32 / 对话8 / 混合0 | 叙事47 / 对话0 / 混合0 |
| 对话比例 | 2.8% | 0.0% |
| 当前 CLI 确定性评分 | 65/100（C级） | 77/100（C级） |
| 错误级违规 | 0 | 0 |
| 警告数 | 8 | 5 |

**before 警告集（仍含四项读者警告 + 主角工具人）**：读者负担 / 主角工具人 / 字数不足 / 质感多样性（凉8次）/ 句子过长 / 开头缺锚点 / **对话缺乏碰撞** / **角色台词差异化**。

**after_v2 警告集（四项读者警告全消失）**：拍内断句 / 字数不足 / 质感多样性（凉6次）/ 碎句病×2。

## 三、#65 等长结论判定（核心 claim 逐条核真身）

| 8-27 报告 claim | 当前代码实测 | 判定 |
|---|---|---|
| after_v2 = 0 error | ✅ 0 error（通过） | **成立** |
| before 四项读者警告（对话缺乏碰撞/角色台词差异化/开头缺锚点/句子过长）在 | ✅ 四项均在（#5/#6/#7/#8）+ 主角工具人#2 | **成立** |
| after_v2 四项读者警告全消失 | ✅ 警告集中无此四项 | **成立** |
| 增益来自铁则本身、非篇幅变短（等长 2238 vs 2384，差 6.1%） | ✅ 等长下 0 error 双章一致，before 读者警告未因缩短而消失 | **成立** |

**关键强化（消除 8-27 §七 方法学混淆）**：
- 8-27 报告 §七 曾标"after_v2 对话缺乏碰撞消失 partly 因检测器引号盲区"。8-27 §⑬ 已落地 Item1（无引号双通道 `checkDialogueConflict`），使无引号对话可被识别。当前实测：before（引号对话）`对话缺乏碰撞` 正常报出（45 轮次无碰撞）；after_v2（无引号对话）`对话缺乏碰撞` + `主角工具人` 均**未报**——证明 after_v2 的"消失"现在是**真消失**（碰撞/私心被真实识别，而非盲区跳过），8-27 §七 顾虑已被 Item1 解决。
- 即：#65 读者爽点增益在"旧检测器盲区"假设被剔除后，结论反而更稳。

## 四、8-29 代码变更带来的 delta（标注，非回归）

1. **② 叙述口径对 ch29_after_v2.md 无影响**：沙盒 0 对话（对话0段），叙述口径 = 全文口径；凉 6 次在叙述段触发 `质感多样性`，与 8-27 报告列出的 after_v2「质感多样性」警告一致，未新增。
2. **① + R4**：2238（ratio 0.746）/ 2384（ratio 0.795）均 ≥ 0.7 → 0 error；与 8-27「短而密放行」等价（现改为"低于100%目标" warning，仍 0 error）。无门禁回归。
3. **质感多样性·凉（已知可接受 FP）**：原章 8 次 / 沙盒 6 次均触发，属 U2「凉节点排除词表」待办项（核心概念词，CLI 树 `threshold_overrides` 够不着，需新机制）。不在本次门禁回归范围，列为已知。
4. **评分仪器不可直接比对**：当前 `check-chapter.ts` 输出来自确定性评分器（100 − err×10 − warn×4 − info×1）；8-27 报告 C69/C61 来自非确定性 health-report/评分器（8-27 §⑩ 已坦诚"门禁不产评级"）。两者非同一仪器，分数（69→77 / 61→65）不可直接比对；但**方向一致（after > before）**在两仪器下均成立，与 #65 增益吻合。门禁相关事实（0 error + 警告集）完全一致。
5. **碎句类命名 delta**：8-27 after_v2 列「碎句×2」，当前为「碎句病×2」（碎句 detector 不再触发、碎句病触发），属检测器演进，非门禁回归。

## 五、结论

#65（铁则二十一·读者爽点本能）经「短版 + 等长版 + 8-29 代码变更后重跑」三重实证，**结论不变且更稳**：
- 等长（2238 vs 2384，差 6.1%）下 after_v2 仍 0 error、before 四项读者警告全在而 after_v2 全消失；
- 8-27 §七 引号盲区顾虑已被 Item1 解决，after_v2 的"四项消失"现为真识别驱动；
- 8-29 三处检测树改动（①②+R4）未对 ch29 等长复验引入任何门禁回归。

**遗留（非本次回归范围）**：U2「凉节点排除词表」机制（原章/沙盒均触发质感多样性·凉，属核心概念词已知 FP），按 8-29 收尾结论列为独立设计轮。

## 六、复现命令与交付

- 沙盒：`npx tsx 检测工具/check-chapter.ts "C:/Users/admin/Desktop/写作引擎产品/InkWeave/_sandbox/ch29_after_v2.md" --target 3000`
- 原章：`npx tsx 检测工具/check-chapter.ts "C:/Users/admin/Desktop/写作引擎产品/InkWeave/项目_裂日/章节/第29章 弧心.md" --target 3000`
- 两章均 EXIT=0、0 error，未加 `--save-fingerprint`，指纹链零污染。
- 代码核真身锚点：`检测工具/checkers.ts` :377-385（②）、:1201-1216（①+R4）。
