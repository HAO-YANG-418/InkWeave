# 回归验证 · 感官密度阈值 CLI 树对齐（#3 收尾）

- 日期：2026-08-29
- 类型：CLI 验收树 `sense_density_low` 阈值对齐（阈值源第三处）
- 执行方：代码方（用户亲自下场改 .ts）

## 一、改动（仅 1 行）

| 文件 | 行 | 改前 | 改后 | 说明 |
|---|---|---|---|---|
| `检测工具/checkers.ts` | 572 | `if (density < 0.02 && stats.totalWords > 1000)` | `if (density < 0.045 && stats.totalWords > 1000)` | 仅阈值 0.02→0.045，**保留 `> 1000` 地板**（8-28 Y 降噪刻意决策，不反向推翻） |

> 检测器 `checkSenseDensity`（:567-578）其余逻辑未动；CLI 树 `sensoryMentions` 计数已在本会话 #2 统一为单字集，与源码树同口径。

## 二、为什么是 0.045（单位与双源背景）

- 检测器语义：`density = totalSensory / totalWords`（0–1 比率），旧 `0.02` 对本文集恒低于真实分布（min 0.038），等于死警告。
- 0.045 ≈ 45 感官/千字，卡在观测 min(38/千字) 上方一点，只标最稀尾部（5/47≈10.6%），恢复"密度过低"本意。
- 此值在本会话 #3 已用于**双源**（源码树 `types.ts:229` + KB `default.json:13`），CLI 树为**第三处硬编码**，本次对齐令三处阈值语义统一。

## 三、验证（不盲认，均实跑）

| 核验点 | 结果 |
|---|---|
| `npm run build` | EXIT=0（tsc 干净，dist 已重编译） |
| CLI `sense_density_low` 命中 | **5/47**：ch17 / ch22 / ch41 / ch44 / ch45（每行 `cli_sense_density_low=1`） |
| 与源码树重合度 | **100% 重合**（源码树同 5 章；两树计数口径已统一） |
| `> 1000` 地板影响 | 5 章字数均 >1000（2729/2855/2107/2111/2117）→ 地板未排除任何一章；若某稀尾章 <1000 字则 CLI 标 4 章（地板故意分歧，非 bug） |
| CLI 树 error 合计 | **6**（ch32–37，与改前一致，零回归） |
| 源码树 error 合计 | **50**（与改前一致） |
| 38–47 零 error | ✅（`sense_density_low` 为 warning 级，不破零 error 判据） |

## 四、结论

- #3 `sense_density` 重标定：**源码树双源 + CLI 树三处**现已全部对齐到 0.045，两树 `sense_density_low` 命中 **5/47 完全重合**，零回归。
- 唯一已知残留债：CLI 树 `> 1000` 地板（8-28 设计决策），属故意分歧，本期按方案方拍板保留。
- 数据来源：`_sandbox/profiler_dual_tree.mts 1 47` → `docs/方案与验收/剖面_1-47.csv`（列 `cli_sense_density_low`）。
