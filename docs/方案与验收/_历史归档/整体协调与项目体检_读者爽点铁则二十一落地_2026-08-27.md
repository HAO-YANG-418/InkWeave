# InkWeave 整体协调 · 项目体检 · 归档报告（2026-08-27）

> 本文档是「裂日 28–37 章十章实战」收尾工作的归档。核心结论：**交付物是 InkWeave 写作引擎这个 skill（生成 + 双树质检 + 反向闭环），《裂日》章节仅为压力测试沙盒，不是要交付的小说。**

## 一、本轮实战验证了引擎哪些能力（十章 = 测验数据）

| 章 | 标题 | 门禁 | 评分 | 感官侧重 |
|---|---|---|---|---|
| 28 | 汇流 | 0 error | C59 | 范本章（锚定写作基准） |
| 29 | 弧心 | 0 error | C61 | 味觉 |
| 30 | 残频 | 0 error | C60 | 视觉 |
| 31 | 粮鸣 | 0 error | C76 | 听觉 |
| 32 | 映殇 | 0 error | C59 | 触觉 |
| 33 | 残熄 | 0 error | C78 | 嗅觉 |
| 34 | 阀前 | 0 error | C73 | — |
| 35 | 醒童 | 0 error | C64 | — |
| 36 | 七管 | 0 error | C69 | — |
| 37 | 废井 | 0 error | **B87** | — |

**结论**：初稿闭环（写前分析 → 主机 LLM 生成 → 双树检测 → 重写 → 反向闭环）真实跑通；写作基准（创作本能级）铁则生效，"不硬凑 / 零 error"达标。

## 二、修掉的引擎缺陷（真成果）

- **BUG②（代码方）**：`源码/anchor-detector.ts` 词表缺失时返回 0 锚点，与 CLI 树口径不一致导致误报；加 fallback 后锚点 0→150，第27章 66→96。
- **推翻两个旧假设（已回填长期记忆）**：① 检测项目名是 `裂日`（非"项目_裂日"）；② D4 字数门禁实为「短而密放行」——密度够即 0 error，非 "<2700 一律 error"。

## 三、#65 整体协调 —— 读者爽点做进生成本能（已落地）

在 `InkWeave写作工坊/生成指令.md` 新增 **铁则二十一 · 读者爽点本能（创作本能级）**，对冲读者视角四个差距：

| 要点 | 治的短板 | 覆盖范围 |
|---|---|---|
| 主角私心驱动（抗工具人） | 主角工具人、被动无私心 | 全局强制 |
| 对话必须碰撞（禁汇报式） | 台词像汇报、缺火花 | 全局强制 |
| 设定埋进危机反应（show don't tell） | 设定解释 > 呈现 | 全局强制 |
| 章末情绪钩子（禁任务续集） | 章末钩子弱 | 强化铁则十/十九 |

## 四、项目体检发现的全部问题（不止 #65 四项）

### A. 引擎能力缺口（检测侧未覆盖）
- ①主角私心驱动、②对话碰撞张力：无对应 ruleId，仅依赖生成侧铁则自觉，门禁不拦。属设计取舍，非 bug。
- ③设定危机呈现（`explain_tone` 部分覆盖）、④章末情绪钩子（`emotion_arc`/`unsaid_gap` 间接覆盖）基本够用。

### B. 文件夹散落（本次整理目标）
- 顶层：`b1_smoke.ts`/`r1_smoke.ts`/`r3_smoke - 实际为 r3_smoke.ts`/`r4_smoke.ts`、`empirical_ai_flavor_check.mjs`、`SKILL_v1_backup.md`、`inkweave.mjs`（入口，保留）。
- `检测工具/`：`_ab_demo_matrix.json`、`_ab_v2_matrix.json`、`d_smoke_smoke.ts`（实为 d_smooth_smoke.ts）、`r2_smoke.ts`。
- 顶层 `交付物/`（6 项）、`_ab_demo/`、`AB_重写v2/`（A/B 测试副本）。
- 顶层 `SKILL.md` 与 `InkWeave写作工坊/SKILL.md` 重复（待确认权威版）。

### C. 结构重叠
- 顶层 `交付物/` 与 `docs/` 体系重叠。
- `dist/知识库/节点/*.md` 与源 `知识库/节点/*.md` 双份（编译产物，无 git 故不处理）。

### D. 铁则体系小一致性
- `二、铁则` 主节编号跳 15→18（十六/十七在 〇.六节），易惑。
- 禁令区未呼应铁则二十一（无"对话无碰撞禁用""主角工具人禁用"提示）。

## 五、归档与整理动作（已执行）

**安全已移动（备份后）**：
- `b1_smoke.ts`/`r1_smoke.ts`/`r3_smoke.ts`/`r4_smoke.ts`、`检测工具/d_smooth_smoke.ts`/`r2_smoke.ts` → `源码/_archive/smoke/`
- `empirical_ai_flavor_check.m,js` → `docs/度量协议/`
- `SKILL_v1_backup.md`、`检测工具/_ab_demo_matrix.json`、`检测工具/_ab_v2_matrix.json` → `docs/历史评审/`
- `交付物/` 6 项 → `docs/历史交付物/`
- `_ab_demo/`、`AB_重写v2/` → `docs/历史评审/ab_tests/`

**保留未动（需你拍板）**：
- `inkweave.mjs`（入口）、`dist/`、`install.bat`、`package*.json`、`tsconfig.json`、`vitest.config.ts` 等保留。

**已解决：顶层 `SKILL.md` 重名冲突（2026-08-27 第二轮）**：
- 体检发现顶层 `InkWeave/SKILL.md` 与 `InkWeave写作工坊/SKILL.md` 共用 `name: "inkweave-writer"`，构成 skill 重名冲突；且顶层为 umbrella 概览、触发词又侵入大纲/设定领地。
- 经核对：三个子模块（写作工坊/大纲创作院/设定工坊）各自 `name` 唯一（`inkweave-writer`/`inkweave-outline`/`inkweave-worldbuilding`），是真正的功能 skill；代码（`verify-gates.ts`/`package.json`）未硬编码引用顶层 SKILL.md。
- 处理：备份至 `.workbuddy/backup/project_cleanup_20260827/SKILL_顶层_umbrella_20260818.md`，并移至 `docs/历史评审/SKILL_顶层_umbrella_20260818.md`，移出 skill 根消除重名。现存 SKILL.md 仅 3 份且名称唯一。

**备份位置**：所有受影响文件已先备份至 `InkWeave/.workbuddy/backup/project_cleanup_20260827/`，确认无误后再做整理，未做任何删除。

**验证**：整理后顶层仅剩正规项目文件（SKILL.md / VERSION.md / inkweave.mjs / install.bat / package* / tsconfig / vitest.config / .gitignore / .inkweave.llm.example.json），冗余项全部清空。

## 六、未决风险与下一步
1. ~~顶层 `SKILL.md` 重复~~ → 已解决（见上，移出 skill 根）。
2. 检测器补缺（对话碰撞/私心驱动正则+LLM语义层）可作为后续升级，本轮未做。
3. 铁则/禁令一致性补强（跳号注释、禁用提示）可选。
4. 回归验证：可用新铁则重跑某章证明 #65 生效（沙盒数据验证）。
