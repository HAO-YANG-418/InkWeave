# 连章真实质量与修复进展（2026-09-02）

## 一、重大纠正：坑1 修复后暴露真实质量
上一轮审计结论"全5章0 error"是**假象**。`check-all` 只跑 CLI 树（`检测工具/checkers.ts`），不合并生成时源码树（`源码/checker.ts` 的 R1 聚合），把源码树的 error 全藏了。
本轮把 `check-all` 也合并源码树后，真实盘面暴露：**只有 ch5（独立矿工 demo）合格，连章 都市_许照 4 章全是 D 级、多 error**。

> 坑1 修复的价值正在于此——它让"全绿"的假象消失，这才是不骗人的出货闭环。

## 二、5 章实时门禁现状（--target 2800，两命令已统一）
| 章节 | 评分 | 等级 | 错误 | 警告 | 提示 | 字数 |
|---|---|---|---|---|---|---|
| 第5章_实战测试 | 85 | B | 0 | 3 | 3 | 3009 |
| 都市_许照_第01章 | 26 | D | 0 | 17 | 6 | 3154 |
| 都市_许照_第02章 | 47 | D | 1 | 10 | 3 | 3180 |
| 都市_许照_第03章 | 15 | D | 3 | 12 | 7 | 3552 |
| 都市_许照_第04章 | 39 | D | 1 | 10 | 11 | 3458 |

注：单章 `check-chapter` 与 `check-all` 现对同章结论一致（坑1已修）。

## 三、本轮已落地
- **#122 字数验收线 2800**：新增 `opt_length_2800` 节点（node-registry / kb-loader / 新建 kb.json），base-prompt 指令"3000–3500"改"2800–3200"，`npx tsc` 重建 + grep 验证 dist 三处 + 新 kb.json 均同步。
- **#120 门禁双路径统一**：`check-all.ts` 加入源码树 import + R1_MERGED_CONFIG + `runSourceTreeCheck`，串行检测合并两树；新增 `--target`（默认 2800）。两命令不再藏警告，已验证 ch5 两路径一致（均 B85/3警告）。
- **#123 ch01 补强**：拆 L19/L71 的"像"簇 + L43 否定"像"→"不似"，消除 2 个"连续比喻过密"error；再把排比"听见"×4 改为多样表述。结果：D1(2error) → D26(0error)，**通过门禁**。

## 四、待修（不阻断门禁，但拉质量/出货样相）
- **ch02**：连续比喻过密×1(error) + 排比"听见"×3 + 碎句 + 连续对话过多×2
- **ch03**：字数硬约束×1(error) + 连续比喻过密×2(error) + 连续对话过多×4 → **3 error**
- **ch04**：字数硬约束×1(error) + 连续对话过多 + 情感弧线缺失
- **ch01**：17 警告待降（→B级需重写级打磨：碎句/连续对话×2/碎句病/连续段落无反转/"的"字密度）

> ch03/ch04 的"字数硬约束" error：源码里是 `maxWords` 类上限约束，旧章写 3500+ 被罚（疑似注水/超上限）。默认 2800 节点写的章（~2800字）不会触发，属旧章遗留。

## 五、检测器核实（修正上一轮审计误判）
- **数据锚点双报 = 误判（非 bug）**：`checkers.ts:742` 高密度(`>5`)/`:745` 低密度(`<0.5`)是互斥 `if`，同章不可能既高又低。上一轮审计写"同章双报"是我看串了，不改检测器。
- **台词差异化统计 quirk**：真实存在（占比49% vs 基础1.2% 矛盾，且数的是标签类型非声口）。标注不修——动它有引入新 error 风险，留待检测器专项。

## 六、下一步建议（待用户拍板优先级）
- 路线A：逐章重写 ch02–04 到 0 error（重活，约 3 章，与 ch01 同款"拆像簇+降排比+插对话动作"）。
- 路线B：把 ch01 的 17 警告降到 B 级（重写级打磨）。
- 当前 ch5 已是合格出货样板；连章需补到至少 0 error 才能当宣传样本。
