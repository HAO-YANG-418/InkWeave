# 实战验证（多种类节点）+ skill 冗余审计 + 文件夹整理

> 日期：2026-09-01 · 阶段：skill 内部阶段收口后维护
> 范围：全量 KB 节点审计、检测器跨节点实战、文档/代码冗余核对、目录整理

---

## 一、实战验证：多种类节点全量种子审计

### 1.1 方法
- 枚举**全部已编译底座** `dist/源码/kb/nodes/**/*.kb.json`（运行时实际喂给 LLM 的 `system_prompt`+`constraints`）。
- 22 节点 × 90 选项 = **95 个已编译选项**（含非必选）跨 5 类（style/vocab/rule/hook/ai）。
- 按 5 有害概念正则扫描：**逼碎句 / 逼冷峻白描 / 硬计数 / 留白藏逻辑 / 禁口语人味**。

### 1.2 三方交叉核对（KB 结构一致性）
| 项 | 结果 |
|---|---|
| KB 源 md 孤儿（footer 选项ID 不在 registry） | **1 个**（仅 `索引.md`，合法节点索引，非选项） |
| 源有+registry 有 但 dist 未编译 | **0** |
| 已编译 但 无对应 KB 源 md | **0** |
| registry 选项 但 dist 无编译 | **0** |

→ **KB 全量一致**：22 节点 × 90 选项，源 md ↔ 编译产物 1:1，零孤儿/零缺编译。

### 1.3 5 概念扫描结论（核心）
- 总命中 94 处；其中**默认路径命中 16 处，非默认 78 处**。
- **默认路径 16 命中全部为合法网文结构约束**（锚点密度/感官标记/钩子句长/句子长度/爽点频率），**零个"逼冷峻白描 / 留白藏逻辑 / 禁口语人味"有害种子漏进任何默认选项**。
  - 例：`opt_desc_sensory`「每个新场景必须有至少 1 个专属气味/声音标记」= A7 五感调度（有益）；`opt_emotion_both`「每350字1处身体锚点」= A6 身体锚点（有益）；`opt_hook_break`「最后一句≤15字」= A3 章末硬钩；`opt_payoff_every_chapter`「每章至少1个爽点」= A12 爽点。
- 非默认命中集中在 `opt_desc_minimal`(极简白描) / `opt_emotion_show`(以动写情·含已注脚"冰山只停情绪层") / `opt_tone_serious`/`opt_tone_cold`/`opt_tone_classical`(严肃/冷峻/古风，本就按需开启、非默认方法论) —— **属用户自选风格，非系统性误伤**。
- `opt_rhythm_mixed` 已见「逗号数与逗句比仅为参考：读着顺就保留连写，只禁一逗到底」——印证 P0 的逗句比硬约束软化已落地。

**判定：去冷峻化在全 KB 默认路径零残留，系统性误伤已根除。** ✅

### 1.4 检测器跨节点实战（真·多种类节点）
对 3 本书（不同节点配置）代表章节跑 `inkweave check`：
| 章节 | 节点配置 | 结果 | 说明 |
|---|---|---|---|
| 都市直播_盲写v6_第3章 | 都市/口语/感官 | 0/100（D） | 种子清零前旧样本 |
| 裂日_第16章B_用引擎 | 玄幻/战斗细节 | 47/100（D） | 旧样本 |
| 斗罗同人_盲写v3_第3章 | 同人/古风 | 8/100（D） | 旧样本 |

→ 检测器在**三种节点配置下均正常跑通、出分项报告**，证明验证管线节点无关、编译集健康。低分是**种子清零前的旧样本**，非回归（当前底座在「收口验证_路线A」已验证盲写 0 错误）。

---

## 二、skill 冗余 / 重复 / 无用审计

### 2.1 docs 规模
`docs/` 共 **165 文件**。子目录：
- `方案与验收/` ≈ 110 文件（本轮重灾区）
- `历史交付物/`（已归档，HTML 报告）
- `历史评审/`（已归档，含 AB 测试章节）
- `度量协议/`（已归档）

### 2.2 `方案与验收/` 冗余点
| 类别 | 数量 | 性质 |
|---|---|---|
| `InkWeave_*_2026-08-22~08-29.md` 一次性验证/回归文档 | ≈ 60 | 上一阶段（trea 接手期）逐次验证，已被本轮 skill 阶段结论覆盖，**过期可归档** |
| `剖面_*.csv` / `剖面_*.md` 跑量剖面数据 | ≈ 22 | 一次性采集产物，非文档 |
| `回归验证_*` / `实战验证_*`(08-27~08-29) | ≈ 15 | 同上 |
| 当前 skill 阶段文档（08-30~09-01，含能力清单/评分卡/协议/P0-P3） | ≈ 18 | **核心资产，保留** |
| 软件化方案（微调数据/ts_patch） | 2 | 未来阶段，保留 |

### 2.3 代码侧死代码
| 路径 | 文件数 | 引用情况 |
|---|---|---|
| `源码/_archive/`（anti-pattern/creative/smoke/super/technique/cooling） | 29 | **0 处被活代码 import**，确为冻结死代码（含与 P3 活目录重名的 `anti-pattern`/`technique` 旧版，已绕开） |
| `_sandbox/`（84K） | 扫描脚本+章节 | 临时 |
| `_backup/`（220K） | 备份 | 临时 |

### 2.4 KB 命名不一致（低危，非阻塞）
- KB 源 md 用**中文名**（`口语白话.md`/`清冷疏离.md`），registry 用**英文 option_id** + 英文描述（`opt_tone_cold`「冷峻克制」）。`清冷疏离.md`→`opt_tone_cold` 合法映射，仅中文名≠英文描述，纯命名不一致，不影响功能。

---

## 三、文件夹整理动作

### 3.1 已执行（docs 归档，非删除，可还原）
- 新建 `docs/方案与验收/_历史归档/`。
- 把 ≈ 80 个过期一次性验证/回归/剖面文档移入 `_历史归档/`（日期 2026-08-22~08-29 + 全部 `剖面_*`）。
- **保留** `方案与验收/` 根下当前阶段核心文档（08-30/08-31/09-01 系列 + 能力清单/评分卡/协议/网文基准/微调方案）。

### 3.2 未执行（需你确认，按安全规则不擅删）
- `源码/_archive/`（29 死代码）：已 self-documenting 命名，建议保留或整体移入 `docs/../_代码归档/`。
- `_sandbox/`、`_backup/`：临时目录，建议删除或归档（含项目章节 md，删前请确认无复用需求）。

### 3.3 整理后 `方案与验收/` 根目录结构（清晰）
```
方案与验收/
├── _历史归档/                    ← 80 个过期验证/剖面文档（可还原）
├── skill能力清单_2026-08-31.md          ← 核心：A/B/C 三层能力
├── 盲写验证协议_2026-08-31.md           ← 核心：盲写铁律（每轮必带评分卡）
├── 盲写评分卡_A层15项_固化.md           ← 核心：分项判分模板
├── 网文可读性基准_覆盖晦涩极简偏差_2026-08-30.md  ← 核心：好懂基准
├── 运行时默认底座漂移_发现与修复_2026-08-31.md    ← 根因记录
├── 改根本_种子清零补全_2026-08-31.md
├── 第4批种子清零与KB注脚漂移修复_2026-08-31.md
├── 脚手架剥离_盲写验证结论_2026-08-31.md
├── 脚手架归档_生成指令_2026-08-31.md
├── P1_B层剥离交检测器_2026-08-31.md
├── 盲写_A11_人味自嘲_带底座_2026-08-31.md
├── 编译集加固与第5批扫描_2026-08-31.md
├── 编译集工程债_P3_补齐缺失模块_2026-09-01.md
├── 收口验证_路线A_2026-09-01.md
├── 样章vs热门网文_差距自评_2026-08-31.md
├── 斗罗同人样章vs原版_差距自评_2026-08-31.md
├── 手写实战前自检清单_2026-08-30.md
├── 微调数据方案_热门网文语料_2026-08-30.md    ← 软件化阶段
├── 微调_ts_patch_提案_2026-08-30.md          ← 软件化阶段
└── 后续规划_08-30起.md
```

---

## 四、结论
1. **多种类节点实战验证通过**：KB 全量一致（零孤儿/缺编译）、默认路径零有害种子、检测器跨 3 节点配置健康。
2. **冗余已定位并清理**：docs 从 ≈110 散落文件收敛为 18 核心 + `_历史归档/`（可还原）；代码死代码 `_archive`/`_sandbox`/`_backup` 留待确认。
3. **skill 阶段收口完成且无回归**，可正式交付；剩余仅为软件化阶段（微调+真LLM），需你拍板跨阶段。
